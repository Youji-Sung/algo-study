# 이상한 기호

## 전지성

```cpp
#include <iostream>
using namespace std;

int main() {
    long long A, B;
    cin >> A >> B;
    cout << (A + B) * (A - B);
    return 0;
}
```

## 성유지
롱롱
```cpp
#include <iostream>
using namespace std;

int main() {
    long long A, B;
    cin >> A >> B;
    cout << (A + B) * (A - B);
}
```