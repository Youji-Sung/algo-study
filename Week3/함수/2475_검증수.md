# 검증수

## 전지성

```cpp
#include <iostream>
#include <vector>
using namespace std;

int main() {
    vector<int> nums(5);
    int sum = 0;

    for(int& num : nums) {
        cin >> num;
        sum += num * num;
    }

    cout << sum % 10;

    return 0;
}
```
